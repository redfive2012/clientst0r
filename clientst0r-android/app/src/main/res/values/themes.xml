<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Base app theme applied to TwaActivity.
        AppCompat is available as a transitive dependency.
        The TWA shows web content so the theme only needs to set
        window / status-bar / navigation-bar colours.
    -->
    <style name="Theme.ClientSt0r" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowBackground">@color/splashBackground</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
    </style>

    <!--
        Splash-screen theme applied to MainActivity.
        On Android 12+ the system uses windowSplashScreen* attributes.
        On older versions core-splashscreen back-ports the behaviour.
        postSplashScreenTheme switches back to Theme.ClientSt0r after the
        splash exits so that the biometric screen is styled correctly.
    -->
    <style name="Theme.ClientSt0r.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.ClientSt0r</item>
    </style>

</resources>
